# App de gestion para adelante

## Build estaticos

```bash
cd styles
yarn install
yarn run build
```

Si es la primera vez copiar los fonts desde `styles/node_modules/material-icons/iconfont` a `application/static/css`

Para el javascript

```bash
cd jsclient
yarn install
yarn run images
yarn build
```
